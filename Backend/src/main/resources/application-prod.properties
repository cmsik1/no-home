# Production security is fail-closed.
# Missing values have no fallback, and weak values are rejected by ProductionSecurityValidator.
auth.jwt.secret=${JWT_SECRET}

# Secure cookies are required in production unless explicitly overridden.
auth.jwt.cookie-secure=${JWT_COOKIE_SECURE:true}
